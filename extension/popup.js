// Silenced by the Algorithm - Popup Script
// Minimal - just handles any future interactions

document.addEventListener('DOMContentLoaded', () => {
  // Popup loaded successfully
  console.log('[Silenced] Popup initialized')
})
